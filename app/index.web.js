import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('MiPTV', () => App);
AppRegistry.runApplication('MiPTV', {
  rootTag: document.getElementById('root'),
});
