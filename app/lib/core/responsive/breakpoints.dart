/// Width, in logical pixels, at or above which the app switches from the
/// phone (single-column list) layout to the tablet (grid / width-capped)
/// layout. Matches Material 3's "medium" window-size-class cutoff and
/// Android's large-screen guidance — comfortably below every tablet's
/// portrait width and above every phone's.
const double kTabletBreakpoint = 600;
