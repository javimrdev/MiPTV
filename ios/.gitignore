# Artefactos generados por `make ios-framework` / `make ios-framework-sim`.
# Se regeneran desde el core de Rust (rust/crates/ffi-uniffi); no se commitean.
/MiPTVCore.xcframework/
/generated/
/xcframework-headers/

# Salida de compilación de Xcode
/build/
/build-device/
/DerivedData/
